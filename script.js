
  function DisplayNumber(DisplayNumber){
    var x = DisplayNumber.value;
    document.getElementsByClassName("Display").innerHTML = x; 
  }
  


    
<script src="script.js">
  const input = document.querySelector("input");
    const buttons = document.querySelectorAll('.Numero');
    for (let button of buttons){
        button.addEventListener('click', DisplayNumber){
            inputNumber(this.Numero)
        }
    }

function DisplayNumber(event) {
        const text = document.querySelector('.Display');
        const number = event.currentTarget.innertext;
        Number(number); 

   const symbols = document.querySelectorAll('.Symbol');

let number;
let symbol; //variable to hold the math symbol; 

switch(=)
if (symbol == +){
    output = choice1 + choice2
}
else if(symbol == -){
    output = choice1 - choice2;
}
else if(symbol == x) {
    output = choice1 * choice2 
}
else if(symbol == ÷){
    output = choice1 / choice2
};

When = is pressed 

if "+"{
    add
}
if "-" {
    subtract
}
if "x" {
    multiply
}
if "÷"{
    divide
}

let msg = $first + $last = 

let choice = input;